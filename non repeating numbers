   map<int,int>mp;
        for (int i=0;i<nums.size();i++)
            mp[nums[i]]++;
        vector<int>ans;
        for (auto pr:mp){
            if (pr.second == 1)
                ans.push_back(pr.first);
        }
        return ans;
